@page
@model Proyecto_Nuevo_Avatar_V2.Pages.ADM10_Cursos.IndexModel
@{
    ViewData["Title"] = "Gestión de Cursos";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
<link rel="stylesheet" href="~/css/ADM_Usuarios.css" />

<h2 class="text-center">Gestión de Cursos</h2>

<p>
    <a asp-page="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Crear nuevo Curso
    </a>

</p>

<form method="get" id="filtroForm" class="mb-4">
    <div class="row g-3 align-items-center">

        <div class="col-md-2">
            <label for="CarreraSeleccionada" class="form-label">Filtrar por carrera:</label>
            <select id="CarreraSeleccionada" name="CarreraSeleccionada" class="form-select">
                <option value="">Seleccione una carrera</option>
                @if (Model.Carreras != null)
                {
                    foreach (var carrera in Model.Carreras)
                    {
                        <option value="@carrera.ID_Carrera" selected="@(Model.CarreraSeleccionada == carrera.ID_Carrera)">
                            @carrera.Nombre
                        </option>
                    }
                }
            </select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Filtrar por nivel:</label>
            <input type="number" name="NivelFiltro" class="form-control"
                   value="@(Model.NivelFiltro ?? (int?)null)"
                   placeholder="1 - 12"
                   min="1" max="12"
                   oninvalid="this.setCustomValidity('El nivel debe estar entre 1 y 12.')"
                   oninput="this.setCustomValidity('')" />
        </div>

    </div>

    <div class="mt-3 text-end">

        <button type="submit" class="btn btn-success" id="btnBuscar">
            <i class="bi bi-search"></i> Buscar
        </button>
            
        <a asp-page="Index" class="btn btn-secondary">
            <i class="bi bi-eraser"></i> Limpiar
        </a>

    </div>
</form>


<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Nombre del Curso</th>
            <th>Carrera</th>
            <th>Nivel</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Cursos != null && Model.Cursos.Any())
        {
            foreach (var curso in Model.Cursos)
            {
                <tr class="align-middle text-center">
                    <td data-label ="" class="text-break">@curso.ID_Curso</td>
                    <td data-label="" class="text-break">@curso.Nombre</td>
                    <td data-label="" class="text-break">
                        @{
                            var nombreCarrera = !string.IsNullOrEmpty(curso.Mensaje) ? curso.Mensaje : curso.ID_Carrera;
                        }
                        @nombreCarrera
                    </td>
                    <td data-label="" class="text-break">@curso.Nivel</td>
                    <td>
                        <a asp-page="Edit" asp-route-id="@curso.ID_Curso" class="btn btn-warning btn-sm">
                            <i class="bi bi-pencil-square"></i> Editar
                        </a>
                        <button type="button"
                                class="btn btn-danger btn-sm"
                                data-bs-toggle="modal"
                                data-bs-target="#confirmDeleteModal"
                                data-id="@curso.ID_Curso"
                                data-nombre="@curso.Nombre">
                            <i class="bi bi-trash3"></i> Eliminar
                        </button>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3" class="text-center">No hay cursos registrados.</td>
            </tr>
        }
    </tbody>
</table>

@if (Model.TotalPaginas > 1)
{
    <div id="paginacion" class="my-3 d-flex justify-content-center">
        <nav aria-label="Paginación de parámetros">
            <ul class="pagination pagination-sm mb-0">
                <!-- Botón Página Anterior -->
                @if (Model.PaginaActual > 1)
                {
                    <li class="page-item">
                        <a class="page-link" href="?numeroPagina=@(Model.PaginaActual - 1)" aria-label="Anterior">
                            <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>
                }

                <!-- Números de Página -->
                @for (int i = 1; i <= Model.TotalPaginas; i++)
                {
                    <li class="page-item @(i == Model.PaginaActual ? "active" : "")">
                        <a class="page-link" href="?numeroPagina=@i">@i</a>
                    </li>
                }

                <!-- Botón Página Siguiente -->
                @if (Model.PaginaActual < Model.TotalPaginas)
                {
                    <li class="page-item">
                        <a class="page-link" href="?numeroPagina=@(Model.PaginaActual + 1)" aria-label="Siguiente">
                            <span aria-hidden="true">&raquo;</span>
                        </a>
                    </li>
                }
            </ul>
        </nav>
    </div>
}


<!-- Modal de resultado -->
<div class="modal fade" id="resultModal" tabindex="-1" aria-labelledby="resultModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header" id="resultModalHeader">
                <h5 class="modal-title" id="resultModalLabel">Resultado</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
            </div>
            <div class="modal-body" id="resultModalBody">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal de confirmación -->
<div class="modal fade" id="confirmDeleteModal" tabindex="-1" aria-labelledby="confirmDeleteModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="confirmDeleteModalLabel">Confirmar eliminación</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                ¿Seguro que deseas eliminar el curso <strong id="cursoNombre"></strong>?
            </div>
            <div class="modal-footer">
                <form method="post" asp-page-handler="Delete">
                    <input type="hidden" name="id" id="cursoIdEliminar" />
                    <button type="submit" class="btn btn-danger">Sí, eliminar</button>
                </form>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const mensaje = "@Html.Raw(TempData["Resultado"])";
            const tipo = "@TempData["TipoMensaje"]";

            if (mensaje && mensaje.trim() !== "") {
                const modal = new bootstrap.Modal(document.getElementById("resultModal"));
                const body = document.getElementById("resultModalBody");
                const title = document.getElementById("resultModalLabel");

                    title.textContent = (tipo === "exito") ? "Éxito" : "Error";

                    if (tipo === "exito") {
                        resultModalHeader.className = "modal-header bg-success text-white";
                    } else {
                        resultModalHeader.className = "modal-header bg-danger text-white";
                    }

                title.textContent = tipo === "exito" ? "Éxito" : "Error";
                body.textContent = mensaje;
                modal.show();
            }

            const confirmDeleteModal = document.getElementById('confirmDeleteModal');
            confirmDeleteModal.addEventListener('show.bs.modal', function (event) {
                const button = event.relatedTarget;
                const id = button.getAttribute('data-id');
                const nombre = button.getAttribute('data-nombre');

                document.getElementById('cursoIdEliminar').value = id;
                document.getElementById('cursoNombre').innerText = nombre;
            });
        });
    </script>
}
